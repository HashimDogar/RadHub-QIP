# Backend configuration
PORT=8080
DATABASE_URL=./data/app.db
ORIGIN=http://localhost:5173
# Email settings are not used in MVP (token is returned by API for pilot convenience)
EMAIL_FROM=
SMTP_HOST=
SMTP_PORT=
SMTP_USER=
SMTP_PASS=
